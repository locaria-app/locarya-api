package com.locarya.adapters.external

// Driven (outbound) adapters to third-party services land here — see ADR 0006.
//
// e.g. the Asaas split-payment client implementing `PaymentGateway[F[_]]`.
// Concrete effects allowed (this is an adapter, outside the core hexagon).
//
// Empty until the first external integration is wired.
